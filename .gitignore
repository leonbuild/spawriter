node_modules

# MCP build output
mcp/dist

# Extension build outputs
ext/build
ext/dist-chrome/
ext/web-ext-artifacts

# prefer package-lock.json
yarn.lock

# "Keep it secret. Keep it safe." - Gandalf the Grey
.env
.env.local

# unified release output (regenerated by npm run build)
release/
