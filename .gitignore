node_modules

# MCP build output
spawriter/dist

# Extension build outputs
extension/build
extension/dist-chrome/
extension/web-ext-artifacts

# Generated icons (built from SVG by convert-icons.js)
extension/src/icons/*.png

# prefer root package-lock.json (workspaces)
yarn.lock
extension/package-lock.json
spawriter/package-lock.json

# "Keep it secret. Keep it safe." - Gandalf the Grey
.env
.env.local
