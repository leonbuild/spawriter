# spawriter

---
description: spawriter - AI-assisted browser automation & debugging for single-spa micro-frontend projects. Use proactively to view, verify, and explore web pages whenever browser context would help.
globs: "**"
alwaysApply: false
---

# spawriter MCP Tools

You have access to **spawriter** MCP tools that control the user's real Chrome tab via CDP (Chrome DevTools Protocol). This is NOT a headless browser — all actions affect the user's visible browser.

**You should proactively use these tools whenever browser context would improve your work.** Do not wait for the user to explicitly ask you to look at the page — if seeing the page would help you give a better answer or verify your work, just do it.

## Available Tools (23)

### Inspection & State

| Tool | Description | Key Args |
|------|-------------|----------|
| `dashboard_state` | Get single-spa app list, statuses, active import-map-overrides | `appName?` |
| `screenshot` | Capture page screenshot, optionally with numbered labels on interactive elements | `labels?` (boolean) |
| `accessibility_snapshot` | Get condensed accessibility tree with search and diff support | `search?`, `diff?` |
| `execute` | Run JS in page context (fast, lightweight, direct CDP) | `code` |
| `playwright_execute` | Run code in Node.js VM sandbox with full Playwright API (page, context, state) | `code`, `timeout?` |
| `console_logs` | Get captured browser console logs with filtering | `count?`, `level?`, `search?`, `clear?` |
| `network_log` | Get captured network requests with filtering | `count?`, `url_filter?`, `status_filter?`, `clear?` |
| `network_detail` | Get full details of a specific request: headers, request/response body | `requestId`, `include?`, `max_body_size?` |

### Debugging & CSS

| Tool | Description | Key Args |
|------|-------------|----------|
| `debugger` | Control JavaScript debugger: breakpoints, stepping, variable inspection | `action` (12 actions), `file?`, `line?`, `condition?`, `expression?`, etc. |
| `css_inspect` | Get computed CSS styles for an element by selector | `selector`, `properties?` (comma-separated) |
| `editor` | View and edit page JS/CSS sources in real-time (hot-reload) | `action` (7 actions), `scriptId?`, `styleSheetId?`, `search?`, `content?`, `line_start?`, `line_end?` |

### Storage & State

| Tool | Description | Key Args |
|------|-------------|----------|
| `storage` | Manage cookies, localStorage, sessionStorage, cache | `action` (9 actions), `key?`, `value?`, `name?`, `domain?`, `origin?`, `storage_types?` |

### Performance

| Tool | Description | Key Args |
|------|-------------|----------|
| `performance` | Runtime metrics, Web Vitals, memory, resource timing | `action` (4 actions), `count?`, `type_filter?` |

### Network

| Tool | Description | Key Args |
|------|-------------|----------|
| `network_intercept` | Intercept/mock/block network requests (Fetch domain) | `action` (5 actions), `url_pattern?`, `mock_status?`, `mock_body?`, `block?` |

### Emulation

| Tool | Description | Key Args |
|------|-------------|----------|
| `emulation` | Device, network, geolocation, timezone, media feature emulation | `action` (8 actions), `width?`, `height?`, `preset?`, `features?`, etc. |

### Page Content

| Tool | Description | Key Args |
|------|-------------|----------|
| `page_content` | Get clean HTML, text, metadata, or search the DOM | `action` (4 actions), `selector?`, `search?`, `max_length?` |

### Navigation & Refresh

| Tool | Description | Key Args |
|------|-------------|----------|
| `navigate` | Navigate the attached tab to a URL | `url` |
| `ensure_fresh_render` | Reload page with fresh cache | — |
| `clear_cache_and_reload` | Granular cache/storage reset + reload. Cookies are always scoped to current page origin (never clears other sites' cookies). | `clear?` (comma-separated types), `origin?`, `reload?` (boolean), `mode?` (deprecated) |

### Import-Map Override Management

| Tool | Description | Key Args |
|------|-------------|----------|
| `override_app` | Manage import-map-overrides | `action`: `set` / `remove` / `enable` / `disable` / `reset_all`; `appName`; `url` (for set) |

### App Lifecycle Control

| Tool | Description | Key Args |
|------|-------------|----------|
| `app_action` | Control single-spa app lifecycle | `action`: `mount` / `unmount` / `unload`; `appName` |

### Session & Connection

| Tool | Description | Key Args |
|------|-------------|----------|
| `session_manager` | Manage multiple Playwright executor sessions | `action`: `list` / `create` / `switch` / `remove` / `remove_all`; `sessionId?` |
| `reset` | Reset CDP + Playwright connections, clear all logs/network/debugger/sessions | — |

---

## Connection Awareness

Before using any spawriter tool, you must have an active browser connection. If a tool call fails with a connection error or timeout:

1. **Ask the user to connect** — say: "I need browser access to [reason]. Please make sure spawriter is connected to your Chrome tab (click the spawriter toolbar button to attach)."
2. Call `reset` to re-establish the connection, then retry.
3. If still failing, guide the user: check that the MCP server is running (`node dist/cli.js serve`), the extension is loaded, and the tab is a normal web page (not `chrome://` or `edge://`).

**Proactively request connection** when you anticipate needing browser access — don't wait until a tool call fails. For example, if the user asks you to fix a UI bug, immediately check if the browser is reachable before diving into code.

---

## Proactive Browser Engagement

**This is the most important section.** You MUST actively use spawriter tools in these situations — do NOT wait for the user to ask:

### When to proactively use the browser

| Situation | What to do | Why |
|-----------|-----------|-----|
| **User shares a URL or mentions a page** | `navigate` to the URL, then `screenshot` + `accessibility_snapshot` | Understand what the user is seeing before responding |
| **User describes a UI problem** | `screenshot` + `console_logs { level: "error" }` + `network_log { status_filter: "error" }` | Get real visual + runtime evidence instead of guessing from code alone |
| **You need a design/layout reference** | `navigate` to the reference URL, `screenshot`, study the layout, then come back | Ground your implementation in real visual context |
| **You just made code changes (CSS, HTML, JS, Vue, React, etc.)** | `ensure_fresh_render` → `screenshot` → compare with before | **Always verify your changes visually** — never assume they look correct |
| **User asks "how does X look?"** or **"what's on the page?"** | `screenshot { labels: true }` + `accessibility_snapshot` | Give a concrete answer based on what's actually rendered |
| **You're implementing a UI feature** | Screenshot the current state **before** starting, then screenshot **after** each significant change | Maintain visual awareness throughout the implementation |
| **User mentions an error or unexpected behavior** | `console_logs` + `network_log` + `screenshot` | Gather all runtime evidence in one shot |
| **You're debugging an API issue** | `network_log` → find the request → `network_detail` for headers + body | See the actual request/response, not just the code that sends it |
| **User asks to match a design or another site** | `navigate` to the reference, `screenshot`, analyze, then navigate back and compare | Visual side-by-side is far more reliable than verbal description |
| **You're exploring an unfamiliar page/app** | `dashboard_state` + `screenshot { labels: true }` + `accessibility_snapshot` | Build a mental model of the page structure and available interactions |
| **After setting up or changing an override** | `dashboard_state` + `ensure_fresh_render` + `screenshot` | Confirm the override actually took effect |
| **User says "it doesn't work" or "it looks wrong"** | `screenshot` immediately — see what they see | Don't ask the user to describe it; look at it yourself |

### Verification-after-changes protocol

Whenever you modify code that affects the UI, follow this sequence **automatically**:

1. `ensure_fresh_render` or `clear_cache_and_reload` (if cache might be stale)
2. `screenshot` — capture the result
3. Compare with your expectations
4. If something looks wrong: `console_logs { level: "error" }` + `network_log { status_filter: "error" }` to diagnose
5. Report the visual result to the user ("Here's how it looks now — [describe what you see]")

**Never tell the user "I've made the change, please check it" without checking it yourself first.**

### Reference-gathering protocol

When you need visual context (e.g., user says "make it look like X", or you want to see how a component library renders):

1. `navigate { url: "..." }` — go to the reference page
2. `screenshot` — capture the visual
3. `accessibility_snapshot` or `page_content { action: "get_html", selector: "..." }` — understand the structure
4. Navigate back to the working page
5. Use what you learned to inform your implementation

### Exploration protocol

When you encounter an unfamiliar page or the user asks about page state:

1. `dashboard_state` — understand app-level structure (single-spa apps, overrides)
2. `screenshot { labels: true }` — see all interactive elements with numbered labels
3. `accessibility_snapshot` — get the semantic structure
4. `page_content { action: "get_metadata" }` — understand page identity
5. If needed: `execute { code: "..." }` to query specific DOM state

### When NOT to use the browser

- Pure backend/algorithmic code changes with no UI impact
- User explicitly says they don't want browser interaction
- Configuration file edits that don't affect rendering
- When the user hasn't set up spawriter yet and the task doesn't require it

---

## Recommended Workflow

### 1. Understand current state first

```
dashboard_state           →  see all apps, their statuses, active overrides
screenshot                →  confirm the visible UI
screenshot { labels: true } → labeled screenshot showing interactive elements
accessibility_snapshot    →  get page structure as text
console_logs              →  check for errors
network_log               →  verify API calls
```

### 2. Set up overrides for local development

```
override_app { action: "set", appName: "@cnic/main", url: "http://localhost:9100/app.js" }
```

After setting an override, the extension panel will auto-sync within ~3 seconds.

### 3. Verify changes are effective

```
dashboard_state  →  confirm overrideUrl shows your localhost URL
screenshot       →  confirm the UI reflects your local code
```

### 4. Complex interactions with Playwright

```
playwright_execute { code: "await page.click('button[type=submit]')" }
playwright_execute { code: "state.formData = await page.inputValue('#email')" }
playwright_execute { code: "await page.fill('#name', 'Test User')" }
```

Use `playwright_execute` for: Playwright locators, form filling, multi-step flows, assertions.
Use `execute` for: quick JS evaluation in page context, reading DOM values, calling page APIs.

### 5. Debug with console and network logs

```
console_logs { level: "error" }          →  filter errors only
console_logs { search: "API" }           →  find API-related logs
network_log { status_filter: "error" }   →  find failed requests
network_log { url_filter: "api.example" } →  filter by URL
network_detail { requestId: "R1.2" }     →  get full headers + request/response body
network_detail { requestId: "R1.2", include: "response_body", max_body_size: 5000 }
```

### 6. Track page changes with snapshot diff

```
accessibility_snapshot                    →  first call caches baseline
accessibility_snapshot                    →  second call shows diff
accessibility_snapshot { search: "button" } →  search for specific elements
accessibility_snapshot { diff: false }    →  force full snapshot (no diff)
```

### 7. Debug JavaScript with breakpoints

```
debugger { action: "enable" }
debugger { action: "list_scripts", search: "app.js" }
debugger { action: "set_breakpoint", file: "app.js", line: 42 }
debugger { action: "set_breakpoint", file: "utils.js", line: 10, condition: "x > 5" }
debugger { action: "resume" }
debugger { action: "step_over" }
debugger { action: "evaluate", expression: "localVar" }
debugger { action: "inspect_variables" }
debugger { action: "pause_on_exceptions", state: "uncaught" }
```

### 8. Inspect CSS styles

```
css_inspect { selector: "#header" }
css_inspect { selector: ".btn-primary", properties: "color,background-color,font-size" }
```

### 9. Manage multiple Playwright sessions

```
session_manager { action: "create", sessionId: "feature-test" }
session_manager { action: "list" }
session_manager { action: "switch", sessionId: "feature-test" }
session_manager { action: "remove", sessionId: "feature-test" }
session_manager { action: "remove_all" }
```

### 10. Manage browser storage

```
storage { action: "get_cookies" }                               →  list all cookies
storage { action: "set_cookie", name: "debug", value: "1" }   →  set a cookie
storage { action: "get_local_storage" }                        →  list all localStorage
storage { action: "get_local_storage", key: "theme" }          →  get specific key
storage { action: "set_local_storage", key: "theme", value: "dark" }
storage { action: "clear_storage", storage_types: "local_storage" }
storage { action: "get_storage_usage" }                        →  check quota/usage
```

### 11. Monitor performance

```
performance { action: "get_metrics" }           →  runtime metrics (heap, nodes, timing)
performance { action: "get_web_vitals" }        →  LCP, CLS, INP, FCP, TTFB with grading
performance { action: "get_memory" }            →  JS heap, DOM nodes, listeners
performance { action: "get_resource_timing" }   →  slowest resources by duration
performance { action: "get_resource_timing", type_filter: "fetch", count: 10 }
```

### 12. Edit page sources live

```
editor { action: "list_sources", search: "app" }    →  find loaded scripts
editor { action: "get_source", scriptId: "123", line_start: 1, line_end: 50 }
editor { action: "edit_source", scriptId: "123", content: "..." }   →  hot-reload
editor { action: "search_source", search: "TODO" }  →  search across all scripts
editor { action: "list_stylesheets" }                →  find CSS sheets
editor { action: "get_stylesheet", styleSheetId: "0" }
editor { action: "edit_stylesheet", styleSheetId: "0", content: "body { color: red; }" }
```

### 13. Intercept network requests

```
network_intercept { action: "enable" }
network_intercept { action: "add_rule", url_pattern: "/api/users", mock_status: 200, mock_body: "[{\"id\":1}]" }
network_intercept { action: "add_rule", url_pattern: "/ads/", block: true }
network_intercept { action: "list_rules" }
network_intercept { action: "remove_rule", rule_id: "rule_1" }
network_intercept { action: "disable" }
```

### 14. Emulate devices and conditions

```
emulation { action: "set_device", width: 375, height: 812, device_scale_factor: 3, mobile: true }
emulation { action: "set_network_conditions", preset: "slow-3g" }
emulation { action: "set_geolocation", latitude: 37.7749, longitude: -122.4194 }
emulation { action: "set_timezone", timezone_id: "America/New_York" }
emulation { action: "set_media", features: "prefers-color-scheme:dark" }
emulation { action: "clear_all" }
```

### 15. Get page content

```
page_content { action: "get_html" }                    →  clean HTML (no scripts/styles)
page_content { action: "get_html", selector: "#main" } →  scoped to element
page_content { action: "get_text" }                    →  plain text content
page_content { action: "get_metadata" }                →  title, meta, OG tags, counts
page_content { action: "search_dom", search: "error" } →  find elements by text/attr
```

### 16. Make code changes, then verify

```
ensure_fresh_render  →  reload with fresh cache after code changes
dashboard_state      →  re-confirm state
screenshot           →  visual check
```

### 17. Cleanup when done

```
override_app { action: "remove", appName: "@cnic/main" }
   — or —
override_app { action: "reset_all" }    ← removes ALL overrides at once
```

---

## override_app Actions Reference

| Action | Required Args | What It Does |
|--------|--------------|--------------|
| `set` | `appName`, `url` | Add or update an import-map override (sets localStorage, syncs to panel) |
| `remove` | `appName` | Remove the override from localStorage (panel keeps URL for re-enable) |
| `enable` | `appName` | Re-enable a previously disabled override |
| `disable` | `appName` | Disable an override without removing its URL |
| `reset_all` | — | Clear ALL overrides at once |

After any override change, the extension DevTools panel auto-detects and syncs within ~3 seconds.

## app_action Lifecycle Reference

| Action | What It Does |
|--------|--------------|
| `mount` | Force-mount an app (calls `activeWhenForced(true)` + `reroute()`) |
| `unmount` | Force-unmount an app (calls `activeWhenForced(false)` + `reroute()`) |
| `unload` | Unregister and unload an app completely |

---

## Safety Rules

1. **Only operate on normal web pages** — never target `chrome://`, `edge://`, or extension pages.
2. **Verify state via runtime checks** — use `dashboard_state` and `execute`, not static HTML assumptions.
3. **Screenshot between major actions** — maintain visual awareness of what the user sees.
4. **Don't assume code changes are live** — always confirm via `dashboard_state` that the override is effective.
5. **Use incremental operations** — prefer targeted `override_app` over broad `reset_all`.
6. **Handle page reloads gracefully** — after `clear_cache_and_reload` or `ensure_fresh_render`, wait before querying state.
7. **Parallel calls are safe** — `dashboard_state` + `screenshot` can run concurrently without timeout issues.
8. **Cookie clearing is origin-scoped** — `clear_cache_and_reload` only removes cookies for the current page's origin. Other sites' cookies are never affected, even with `mode: "aggressive"`.

## Troubleshooting

| Symptom | Fix |
|---------|-----|
| Tool timeout | Call `reset` to re-establish CDP + Playwright sessions |
| Override not reflected | Use `ensure_fresh_render` or `clear_cache_and_reload { clear: "cache,service_workers" }` |
| Panel not syncing | Extension detects changes via 3s polling; wait a few seconds or refresh the panel |
| App not mounting after override | Navigate to the app's route first (e.g., `/j/:journalId/my/edit`) then check status |
| `importMapOverrides not available` | Page may not have loaded yet; wait and retry |
| Debugger not paused | Call `debugger { action: "enable" }` first, then set breakpoints and trigger code execution |
| CSS inspect "Element not found" | Verify selector with `execute { code: "document.querySelector('...')" }` first |
| Session limit reached | Sessions auto-evict oldest when max (5) reached; use `session_manager { action: "remove" }` to free slots |
| Stale cached bundle | `clear_cache_and_reload { clear: "cache,service_workers" }` clears cache + service workers without wiping cookies |
| Need to clear cookies too | `clear_cache_and_reload { clear: "cache,cookies" }` clears cache + cookies for current origin only (other sites unaffected) |
| `playwright_execute` connection error | Call `reset` then retry; ensure relay server is running |
| No console/network logs | Logs are captured after CDP connection is established; re-trigger the action |
| Labeled screenshot shows no labels | Page may have no interactive elements; check `accessibility_snapshot` |
